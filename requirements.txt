# Pinned-loose runtime requirements, mirrored from pyproject.toml [project.dependencies]
# for users who prefer `pip install -r requirements.txt` over `pip install -e .`
typer>=0.12
pydantic>=2.6
duckdb>=1.0
pandas>=2.2
pyyaml>=6.0
rich>=13.7
